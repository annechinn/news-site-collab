
  /*
   function: compareNumbers
     numbers: an array of numbers
     number: a number to compare to numbers in the array

   returns a new array that has the appropriate value for each corresponding
   number in the input array. 
   if the input array value 
    equals the number: 'eq'
    is greater than the number: 'gt'
    is less than the number: 'lt'

   compareNumbers([1, 3, 5, 4, 6, 2], 4) returns ["lt", "lt", "gt", "eq", "gt", "lt"]
   compareNumbers([10, 2, 6], 4) returns ["gt", "lt", "gt"]

   */

  function compareNumbers(numbers, number) {
  }


  
